[package]
name = "memo"
version = "0.6.0"
edition = "2021"
description = "Small context-aware memo CLI with one immutable Markdown file per note"
license = "MIT"

[dependencies]
sha2 = "0.10"

[[bin]]
name = "memo"
path = "src/main.rs"
